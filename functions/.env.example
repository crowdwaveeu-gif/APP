# Firebase Functions Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env files to version control!

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# SMTP Email Configuration (Zoho)
# IMPORTANT: Use an App Password from Zoho instead of your main password
# Zoho Account → Settings → Security → App Passwords
SMTP_USER=your-email@crowdwave.eu
SMTP_PASSWORD=your_zoho_app_password_here

# Security Notes:
# 1. Always use App Passwords for SMTP instead of your main account password
# 2. Rotate credentials immediately if they are ever exposed
# 3. Keep this .env file local - it should NEVER be committed to git
# 4. After copying, run: git update-index --assume-unchanged functions/.env
